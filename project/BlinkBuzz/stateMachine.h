#ifndef stateMachine_included
#define stateMachine_included

extern char stateAt; //holds the state which we are at

void state_advance(); //allows the state machine to advance

void state1();//states will hold actions of what will occur
void state2();
void state3();//this will be turned into an assembly hopefully
void state4();
 
#endif 
